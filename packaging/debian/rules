#!/usr/bin/make -f
# Bat verbose de de debug khi build fail
export DH_VERBOSE = 1

%:
	dh $@ --buildsystem=cmake

override_dh_auto_configure:
	dh_auto_configure -- \
		-DBUILD_ADDON=ON \
		-DCMAKE_BUILD_TYPE=Release
